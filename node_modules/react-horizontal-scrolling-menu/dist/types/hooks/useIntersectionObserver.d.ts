import { ItemsMap } from '../ItemsMap';
import { observerOptions } from '../settings';
import { type Refs } from '../types';
declare function useIntersectionObserver({ items, itemsChanged, refs, options, wrapperVisible, }: Props): void;
export default useIntersectionObserver;
interface Props {
    items: ItemsMap;
    itemsChanged: string;
    options: typeof observerOptions;
    refs: Refs;
    wrapperVisible: {
        current: boolean;
    };
}
